---
- name: Get some facts - Date
  hosts: all
  gather_facts: true
  tasks:
    - name: Print hostname
      ansible.builtin.debug:
        var: ansible_facts['hostname']
    - name: Print time
      ansible.builtin.debug:
        var: ansible_date_time['iso8601']
